psutil>=5.9
plyer>=2.1

# Windows-only toast backends (tried before plyer). Ignored on Linux/macOS.
win11toast>=0.35; sys_platform == "win32"
winotify>=1.1.0; sys_platform == "win32"

# System tray / taskbar icon (tray.py). Not needed for the plain CLI monitor.
pystray>=0.19
Pillow>=9.0
